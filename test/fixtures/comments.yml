rickshaw_comment_one:
  author: owner_six
  commentable: rickshaw (Discussion)
  comment_token: <%= SecureRandom.hex(4) %>
  comments_total: 1

rickshaw_comment_two:
  author: confirmed_linked_owner
  commentable: rickshaw (Discussion)
  comment_token: <%= SecureRandom.hex(4) %>

rickshaw_comment_three:
  author: confirmed_linked_owner
  commentable: rickshaw_comment_one (Comment)
  comment_token: <%= SecureRandom.hex(4) %>

walking_track_comment_one:
  author: confirmed_linked_owner
  commentable: walking_track (Discussion)
  comment_token: <%= SecureRandom.hex(4) %>
  comments_total: 1

walking_track_comment_two:
  author: owner_six
  commentable: walking_track (Discussion)
  comment_token: <%= SecureRandom.hex(4) %>

walking_track_comment_three:
  author: owner_six
  commentable: walking_track_comment_one (Comment)
  comment_token: <%= SecureRandom.hex(4) %>
