<% if @tender.published? %>
  <%= turbo_stream.replace(
    "tender-buttons-section",
    partial: "published_tender",
    locals: { tender: @tender }
  )%>
<% end %>

<%= turbo_stream.update "flash-messages" do %>
  <%= flash_messages %>
<% end %>
