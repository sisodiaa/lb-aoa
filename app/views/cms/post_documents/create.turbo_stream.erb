<%= turbo_stream.append(
  "post-documents",
  partial: "post_document",
  locals: { document: @document, post: @post }
)%>

<%= turbo_stream.replace(
  "post_document",
  partial: "form",
  locals: { post: @post, document: @new_document }
)%>

<%= turbo_stream.update "flash-messages" do %>
  <%= flash_messages %>
<% end %>
