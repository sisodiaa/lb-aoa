<%= turbo_frame_tag :attachments do %>
  <ul class="mb-4">
    <%= turbo_frame_tag "post-documents" do %>
      <%= render(
        partial: "post_document",
        collection: @post.documents.reject(&:new_record?),
        as: :document,
        locals: {post: @post}
      )%>
    <% end %>
  </ul>
<% end %>
