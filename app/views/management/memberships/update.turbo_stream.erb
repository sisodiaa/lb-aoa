<%= turbo_stream.update(
  "#{@status}_memberships",
  partial: "management/memberships/list",
  locals: {status: @status, memberships: @memberships, pagy: @pagy}
) %>

<%= turbo_stream.update(
  "membership-details",
  partial: "management/memberships/details",
  locals: {status: @status, membership: @membership}
) %>

<%= turbo_stream.update "flash-messages" do %>
  <%= flash_messages %>
<% end %>
